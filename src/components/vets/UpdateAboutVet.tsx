import FullScreenModal from "../ui/modal"

export const UpdateAboutVet = () => {
  return (
    <FullScreenModal isOpen={true} onClose={() => {}} title="Acerca de mi" isBack={true}>
      <form>
        
      </form>
    </FullScreenModal>
  )
}
